@model COES.MVC.Intranet.Areas.Equipamiento.Models.FamiliaIndexModel
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<script src="~/Areas/Equipamiento/Content/Scripts/familia.js"></script>
<div class="form-title">
    <div class="content-titulo">Tipos de Equipo</div>
    <div class="content-botonera">
        <input type="button" id="btnBuscar" value="Consultar" />
        <input type="button" id="btnNuevo" value="Nuevo" @Model.EnableNuevo />
    </div>
</div>
<div class="content-hijo" id="mainLayout">
    <input id="hdnEnableEdit" type="hidden" value="@Model.EnableEditar" name="EnableEditar" />
    <input id="hdnEnableNuevo" type="hidden" value="@Model.EnableNuevo" name="EnableNuevo" />
    <table class="content-tabla-search" style="width:auto">
        <tr>
            <td>Estado:</td>
            <td>
                <select id="cbEstadoFamilia" name="EstadoCodigo">
                    <option value=" ">Todos</option>
                    @foreach (var item in Model.ListaEstados)
                    {
                        <option value="@item.EstadoCodigo">@item.EstadoDescripcion</option>
                    }
                </select>
            </td>
        </tr>
    </table>

    <div class="content-tabla">
        <div id="mensaje" style="width:300px; margin:0"></div>
        <div class="tabla-header">
            <div class="table-paginado" id="paginado"></div>
        </div>
        <div class="table-list" id="listado" align="center"></div>
    </div>
    <div style="clear:both; height:30px"></div>
</div>

<div id="popupDetalleFam">
    <span class="button b-close"><span>X</span></span>
    <div class="popup-title"><span>Detalle de tipo de Equipo</span></div>
    <div id="detalleFamilia"></div>
</div>
<div id="popupNuevaFam">
    <span class="button b-close"><span>X</span></span>
    <div class="popup-title"><span>Nuevo tipo de equipo</span></div>
    <div id="nuevaFamilia"></div>
</div>
<div id="popupEditarFam">
    <span class="button b-close"><span>X</span></span>
    <div class="popup-title"><span>Editar tipo de equipo</span></div>
    <div id="editarFamilia"></div>
</div>