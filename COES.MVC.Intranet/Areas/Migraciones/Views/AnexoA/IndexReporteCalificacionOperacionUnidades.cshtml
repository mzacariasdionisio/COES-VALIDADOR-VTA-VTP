@model COES.MVC.Intranet.Areas.IEOD.Models.BusquedaIEODModel
@{
    ViewBag.Title = "Anexo A";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<link href="~/Content/Css/multiselect.css" rel="stylesheet" />
<script src="~/Content/Scripts/multiselect.js"></script>
<script src="~/Areas/Migraciones/Content/Scripts/anexoa/globalFuncionPR5.js?id=1"></script>
<script src="~/Areas/Migraciones/Content/Scripts/anexoa/calificacionHorasOperacionUnidad.js?id=1"></script>
<link href="~/Areas/IEOD/Content/Css/css.css?v=1.6" rel="stylesheet" />

<script src="~/Areas/Migraciones/Content/Scripts/anexoa/cargarmenupr5.js?id=1.1"></script>

@Html.Partial("CargarMenu")
<div class="form-title" style="text-indent:10px">
    <div style="width:auto" class="content-titulo">Calificación de la Operacion de las Unidades de Generación (@Model.FechaInicio)</div>
    @Html.Partial("CargarMenuFilter")
</div>

<div class="content-hijo" id="mainLayout">
    <div class="search-content">
        <table class="content-tabla-search" style="width:auto">
            <tr>
                <td>Tipo de Operación:</td>
                <td>
                    <select id="cbTipoOperacion" name="cbTipoOperacion" multiple="multiple">
                        @if (Model.ListaTipoOperacion.Count > 0)
                        {
                            foreach (var item in Model.ListaTipoOperacion)
                            {
                                <option value="@item.Subcausacodi">@item.Subcausadesc</option>
                            }
                        }
                    </select>
                    <input type="hidden" id="hfTipoOperacion" name="hfTipoOperacion" />
                </td>
                <td>Empresa:</td>
                <td>
                    <select id="cbEmpresa" name="cbEmpresa" style="width:250px;" multiple="multiple">
                        @foreach (var item in Model.ListaEmpresas)
                        {
                            <option value="@item.Emprcodi">@item.Emprnomb</option>
                        }
                    </select>
                    <input type="hidden" id="hfEmpresa" name="hfEmpresa" />
                </td>
                <td> Tipo de Central:</td>
                <td>
                    <div id="tipocentral"></div>
                </td>
                <td> Tipo de Combustible:</td>
                <td>
                    <div id="tipocombustible"></div>
                </td>
            </tr>
        </table>

        <input type="hidden" id="txtFechaInicio" value="@Model.FechaInicio" />
        <input type="hidden" id="txtFechaFin" value="@Model.FechaFin" />
    </div>

    <div class="content-tabla">
        <div style="clear:both; height:5px"></div>
        <div class="table-paginado" id="paginado"></div>
        <div class="table-list" id="listado"></div>
    </div>

    <div style="clear:both; height:30px"></div>

    <div id="idGraficoContainer" style="min-width:310px; height:400px; max-width:800px; margin:0 auto"></div>
</div>
