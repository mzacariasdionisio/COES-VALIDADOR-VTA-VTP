@model COES.MVC.Intranet.Models.HeaderModel

@foreach (var item in Model.ListaFavoritos) {
    <div class="acceso-directo-header-item">        
        <a href="JavaScript:OpenUrl('@item.OpcionURL', '@item.Tipo', @item.OpcionId, '@item.DesControlador', '@item.DesAccion');">@item.Nombre</a> 
    </div>
}